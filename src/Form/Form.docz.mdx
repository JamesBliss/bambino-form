---
name: Form
route: /form
---

import { Playground, Props } from 'docz'
import { object, string } from 'yup';
import Form from '../../src/Form/Form'
import Context from '../../src/Context/Context'
import Input from '../../src/Input/Input'

# Form

The `<Form />` component is the most important element of the puzzle, it controls the flow and logic of all that reside within it.

`<Form />` is the place you set a parent schema, initial data set and the sumbit fuction.

## Props

<Props of={ Form } />

## Basic usage

<Playground>
  <Form onSubmit={ data => alert(JSON.stringify(data, null, 2)) }>
    <Input
      name='name'
      placeholder='Enter name'
    />
    <br />
    {/* This is required because docz doesn't support form submission 🤦‍ */}
    <Context.Consumer>
      { ({ handleSubmit }) => (
        <button onClick={ (e) => handleSubmit(e) }>Submit name </button>
      )}
    </Context.Consumer>
  </Form>
</Playground>

## Initial values

<Playground>
  <Form
    initialValues={ {
      first_name: 'James',
      last_name: 'Bliss'
    } }
    onSubmit={ data => alert(JSON.stringify(data, null, 2)) }
  >
    <Input
      name='first_name'
      placeholder='Enter first name'
    />
    <br />
    <Input
      name='last_name'
      placeholder='Enter last name'
    />
    <br />
    {/* This is required because docz doesn't support form submission 🤦‍ */}
    <Context.Consumer>
      { ({ handleSubmit }) => (
        <button onClick={ (e) => handleSubmit(e) }>Submit full name </button>
      )}
    </Context.Consumer>
  </Form>
</Playground>

## Schema

<Playground>
  <Form
    schema={ object().shape({
      first_name: string().required('First name is required'),
      last_name: string()
    }) }
    onSubmit={ data => alert(JSON.stringify(data, null, 2)) }
  >
    <Input
      name='first_name'
      placeholder='Enter first name'
    />
    <br />
    <Input
      name='last_name'
      placeholder='Enter last name'
    />
    <br />
    {/* This is required because docz doesn't support form submission 🤦‍ */}
    <Context.Consumer>
      { ({ handleSubmit }) => (
        <button onClick={ (e) => handleSubmit(e) }>Submit full name </button>
      )}
    </Context.Consumer>
  </Form>
</Playground>